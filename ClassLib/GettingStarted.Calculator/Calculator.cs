namespace GettingStarted.Calculator
{
    public class Calculator
    {
        public int Sum(string parameter1, string parameter2)
        {
            return int.Parse(parameter1) + int.Parse(parameter2);
        }
    }
}
